language : generic

sudo: required

services:
   - docker
  
before_install:
   - docker build -t neps/react-test-app -f ./frontend/Dockerfile.dev ./frontend
  
script:
   - docker run -e CI=true neps/react-test-app npm run test

after_success:
   - docker build -t neps/docker-frontend ./frontend
   - docker build -t neps/docker-backend ./backend
   - docker build -t neps/docker-nginx   ./nginx


   - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  
   - docker push neps/docker-frontend
   - docker push neps/docker-backend
   - docker push neps/docker-nginx


